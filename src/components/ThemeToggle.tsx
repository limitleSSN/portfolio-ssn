
import React from "react";

const ThemeToggle: React.FC<{className?: string; showLabel?: boolean}> = () => {
  // Component is now a no-op (does nothing)
  return null;
};

export default ThemeToggle;
